# Se construirá la imagen a partir del fichero dockerfile:
build:
  docker:
    web: Dockerfile

# Si no se especifica método run en el heroku.yml, utilizará por
# defecto el comando CMD especificado en el dockerfile.